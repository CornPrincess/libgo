/*
本文件不是“运行逻辑示例”，而是“概念总览说明文件”。
你可以把它当作教程首页来看，后面的 01~12 文件分别把每个知识点拆开演示。

一、什么是协程
1. 协程是一种“用户态的轻量执行单元”。
   它和函数一样，有自己的执行现场；和线程一样，可以在一个任务没法继续推进时切换到别的任务。
2. 协程的调度通常由程序自己控制，而不是完全交给操作系统。
3. 在 libgo 里，协程是“有栈协程（stackful coroutine）”，也就是每个协程都有自己的栈空间，
   因此它可以像普通同步函数一样层层调用、层层阻塞，而不必把整段代码改写成回调地狱。

二、协程和线程的区别
1. 调度者不同
   - 线程：由操作系统调度，属于内核调度对象。
   - 协程：由用户态调度器调度，属于进程内部的执行单元。
2. 切换成本不同
   - 线程切换通常要进入内核，切换寄存器、栈、调度状态，成本更高。
   - 协程切换通常在用户态完成，只切换少量上下文，成本更低。
3. 数量级不同
   - 线程数量太多时，内存和调度开销会明显上升。
   - 协程通常可以开得更多，更适合“一连接一任务”“一请求一任务”这种模型。
4. 阻塞影响范围不同
   - 线程阻塞：如果一个线程阻塞了，这个线程上的执行流整体停住。
   - 协程阻塞：如果阻塞点被协程库接管，通常只是当前协程挂起，线程还能去跑别的协程。
5. 并行与并发的关系不同
   - 线程可以天然映射到多核并行。
   - 协程本身更偏“高并发组织方式”；是否并行，取决于底层是否配合多线程调度。

三、协程的使用场景
1. 高并发 I/O 场景
   - 网络服务
   - 设备接入网关
   - 大量 socket 会话
   - 多个串口/管道/文件描述符的协同处理
2. 同步代码写法，但底层想获得异步效果
   - 你希望写出“先读、再算、再写”的直线式代码
   - 但又不想用回调或状态机把代码撕碎
3. 存在大量等待时间
   - 等网络
   - 等设备
   - 等定时器
   - 等队列
   这类任务最适合协程
4. 线程资源紧张
   - 边缘设备
   - 嵌入式 Linux 盒子
   - 网关类设备
   - CPU 核数少，但连接数/任务数多

四、协程解决了什么问题
1. 把“异步问题”写成“同步代码”
   协程最大的工程价值，不只是快，而是“代码更容易维护”。
2. 降低线程数量
   把“一个任务一个线程”改成“多个任务复用少量线程 + 大量协程”。
3. 降低上下文切换成本
   对于大量短等待任务，协程切换比线程切换通常更轻。
4. 避免回调地狱和复杂状态机
   许多原本要拆成多个回调阶段的逻辑，可以直接顺序写出来。
5. 提高 I/O 密集型场景吞吐
   当阻塞点被 hook 或用协程同步原语替代后，线程不会白白等在那里。

五、有栈协程和无栈协程的区别
1. 有栈协程（stackful）
   - 每个协程有自己的栈
   - 可以在任意函数深度挂起和恢复
   - 写法接近普通同步函数
   - 上手简单，可读性好
   - 单协程内存占用通常高于无栈协程
2. 无栈协程（stackless）
   - 通常没有独立调用栈，状态保存在编译器生成的状态机中
   - 常见形式：C++20 coroutine、手写状态机、回调链
   - 单协程内存通常更省
   - 但代码风格、生命周期和异常路径处理往往更复杂
3. 直观理解
   - 有栈协程：像“线程的轻量版”
   - 无栈协程：像“函数被编译器改写成状态机”

六、有栈协程和无栈协程各自更适合什么场景
1. 有栈协程更适合
   - 业务逻辑层次深
   - 调用链长
   - 需要把阻塞式第三方库直接接进来
   - 希望最大程度复用已有同步代码
   - 团队更重视可读性、维护性、排障效率
2. 无栈协程更适合
   - 状态机很明确
   - 追求极致内存占用
   - 代码从一开始就围绕 async/await 设计
   - 编译器、工具链、生态已完整支持

七、在能源行业 EMS 嵌入式盒子场景，更适合哪种协程
结论先行：
1. 如果你的 EMS 盒子运行的是嵌入式 Linux，且软件里有大量 I/O 等待、协议接入、
   数据采集、上送、设备管理、告警处理，并且要快速交付和稳定维护，那么更适合
   采用“有栈协程”，libgo 这一类方案很契合。
2. 如果你的目标平台资源极端紧张，工具链很新，且你能接受更强的工程约束，
   某些模块可以考虑无栈协程，但它通常不如有栈协程那样利于快速承接历史同步代码。

原因如下：
1. EMS 盒子的典型任务以 I/O 密集和多协议协同为主
   - Modbus TCP/RTU
   - IEC 104
   - MQTT
   - 本地串口
   - 北向平台通信
   - 本地缓存/文件落盘
   这些任务大多“等待多、计算少”，非常适合协程。
2. EMS 项目往往要接大量历史库或厂商 SDK
   - 很多库是同步阻塞接口
   - 有栈协程更容易直接包裹这些逻辑
3. 现场运维要求高
   - 代码需要可读
   - 问题要容易复现和排查
   - 有栈协程的直线式代码对现场维护更友好
4. 核心约束通常不是“单协程再省几百字节”，而是整体稳定性与开发效率
   因此大多数 EMS 边缘控制盒、采集盒、网关盒，优先建议有栈协程。

八、什么时候不建议滥用协程
1. 纯 CPU 密集型长计算，不会因为协程自动变快
   这类任务要靠多线程、多进程、SIMD、算法优化。
2. 中断上下文、硬实时微控制器裸机环境
   这类环境通常不适合引入完整协程调度器。
3. 调度边界和阻塞点完全不可控的库
   如果某个库内部长期阻塞且无法被 hook/隔离，就要谨慎设计线程边界。

九、如何阅读后续示例
1. 01：协程创建与基本调度
2. 02：主动让出 CPU、休眠与协作式切换
3. 03：自定义调度器与栈大小
4. 04：Channel 基本通信
5. 05：Channel 超时、非阻塞、关闭
6. 06：协程锁与读写锁
7. 07：定时器
8. 08：多线程调度
9. 09：协程局部存储 CLS
10. 10：defer 延迟清理
11. 11：协程池
12. 12：连接池
*/

int main() {
    return 0;
}
